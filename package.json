{
  "name": "hotel",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && node -e \"require('fs').cpSync('frontend/dist', 'dist', {recursive: true})\""
  }
}
