{
  "name": "hotel",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && node -e \"require('fs').cpSync('frontend/dist', 'dist', {recursive: true})\""
  }
}
